# Compose para rodar a API localmente ou em servidores Docker simples.
# No EasyPanel você pode usar este compose OU apontar para o Dockerfile direto.
services:
  sema-api:
    build:
      context: .
      dockerfile: Dockerfile
    image: sema-api:latest
    container_name: sema-api
    restart: unless-stopped
    ports:
      - "3001:3001"
    environment:
      PORT: 3001
      APP_DOMAIN: ${APP_DOMAIN:-senma.adv.br}
      SITE_URL: ${SITE_URL:-https://senma.adv.br}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-https://senma.adv.br,https://www.senma.adv.br}
      SUPABASE_URL: ${SUPABASE_URL}
      SUPABASE_ANON_KEY: ${SUPABASE_ANON_KEY}
      SUPABASE_SERVICE_ROLE_KEY: ${SUPABASE_SERVICE_ROLE_KEY}
      SUPABASE_STORAGE_BUCKET: ${SUPABASE_STORAGE_BUCKET:-sema-media}
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3001/health"]
      interval: 30s
      timeout: 5s
      retries: 3
